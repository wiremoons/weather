name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: build-script
      run: sh ./build.sh
    - name: make
      run: cd ./build ; make
  
